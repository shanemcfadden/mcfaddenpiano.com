import * as React from 'react';
import NavBar from '../components/NavBar';

const ListenPage = () => {
    return (
        <div>
            <NavBar />
            <div> Listen Page </div>
        </div>
    );
};

export default ListenPage;
