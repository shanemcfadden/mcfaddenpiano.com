import * as React from 'react';
import PageLayout from '../components/PageLayout';

const ListenPage = () => {
    return (
        <PageLayout>
            <div> Listen Page </div>
        </PageLayout>
    );
};

export default ListenPage;
